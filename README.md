for Archlinux
```
yay -S curl clang aarch64-linux-gnu-binutils arm-linux-gnueabi-binutils inetutils build-essential
```

```
git submodule update --recursive --init
make
# do fix download url in Makefile
```
